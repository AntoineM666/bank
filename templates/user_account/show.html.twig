{% extends 'base.html.twig' %}

{% block title %}UserAccount{% endblock %}

{% block body %}
    <h1>UserAccount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_account.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user_account.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ user_account.date ? user_account.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ user_account.firstname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_account_index') }}">back to list</a>

    <a href="{{ path('user_account_edit', {'id': user_account.id}) }}">edit</a>

    {{ include('user_account/_delete_form.html.twig') }}
{% endblock %}
